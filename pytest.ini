[pytest]
testpaths = tests             # only collect tests/ directory
python_files = test_*.py
addopts = -ra
markers =
    integration: slow test that hits multiple components
    docker: tests that build/run a Docker image (slow)